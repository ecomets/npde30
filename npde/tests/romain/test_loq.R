
  # --------------------------------------------------------------------------------------------------------
  # Plot : xtheo_nocens
  # --------------------------------------------------------------------------------------------------------

  # --------------------------------------------------------------------------------------------------------
  # Plot : xtheo_cens_pd_nan
  # --------------------------------------------------------------------------------------------------------

  # --------------------------------------------------------------------------------------------------------
  # Plot : xtheo_cens
  # --------------------------------------------------------------------------------------------------------

  ## plot loq : inactif car xtheo_nocens@results@ploq = numeric(0)
  #plot(xtheo_nocens,plot.type="loq")

  # --------------------------------------------------------------------------------------------------------
  # Plot : Remifentanil
  # --------------------------------------------------------------------------------------------------------

  # --------------------------------------------------------------------------------------------------------
  # Plot : user guide npde
  # --------------------------------------------------------------------------------------------------------

plot(x50,plot.type="loq")

  # plot loq
  plot(x50,plot.type="loq",

       main = "Probability of values below LOQ versus independent variable",
       size.main = 14,
       sub = "",
       size.sub = "",

       xlab= x50@data@name.predictor,
       ylab= "Pr[Y<LOQ]",
       size.xlab = 12,
       size.ylab = 12,

       xlim=c(),
       ylim=c(),

       grid=TRUE,

       col.pobs="steelblue4",
       pch.pobs=20,
       size.pobs=2,
       alpha.pobs = 0.5,

       col = "black",
       lty = 1,
       lwd = 0.5,
       alpha = 1,

       col.bands = "red",
       lty.bands = 2,
       lwd.bands = 1,
       alpha.bands = 1,

       alpha.bands = 0.25,
       fill.bands = "skyblue",

       size.text.x = 10,
       size.text.y = 10,

       breaks.x = 10,
       breaks.y = 10,

       xlog = FALSE,
       ylog = FALSE)


# ##########################################################################################
# END CODE
# ##########################################################################################






